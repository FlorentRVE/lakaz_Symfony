{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}

{# ========== Formulaire principal, utiliser pour la création et modification des demandes ============ #}

<div class="bg-green-400 p-7 mx-auto my-6 rounded-xl w-4/5 shadow-xl">
    {{ form_errors(form) }}

    {{ form_start(form) }}
        <div class="w-11/12 mx-auto flex flex-col">

            <div class="mb-8 flex flex-col">
                {{ form_label(form.proteine, 'Macros', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                {{ form_widget(form.proteine, {'attr': {'class': 'rounded-lg font-semibold p-4'}}) }}
            </div>
            <div class="mb-8 flex flex-col">
                {{ form_label(form.glucide, 'Macros', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                {{ form_widget(form.glucide, {'attr': {'class': 'rounded-lg font-semibold p-4'}}) }}
            </div>
            <div class="mb-8 flex flex-col">
                {{ form_label(form.lipide, 'Macros', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                {{ form_widget(form.lipide, {'attr': {'class': 'rounded-lg font-semibold p-4'}}) }}
            </div>
            <div class="mb-8 flex flex-col">
                {{ form_label(form.calorie, 'Macros', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                {{ form_widget(form.calorie, {'attr': {'class': 'rounded-lg font-semibold p-4'}}) }}
            </div>

            <button class="btn bg-green-500 rounded-2xl text-center hover:brightness-125 shadow-lg font-bold px-7 py-3 self-end w-1/3 text-lg">{{ button_label|default('Macros') }}</button>
        </div>
    {{ form_end(form) }}
</div>
