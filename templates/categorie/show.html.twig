{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}

    <div class="flex flex-col gap-10 mx-auto w-[95%] mt-10">
        <table class="table">

            <thead>
                <tr class="bg-slate-800 text-slate-100 border-2 border-slate-900 uppercase">
                    <th>Id</th>
                    <th>Label</th>
                </tr>
            </thead>
            <tbody>
                <td class="p-2 bg-slate-50 text-slate-900 text-center border-2 border-slate-900 uppercase font-extrabold">{{ categorie.id }}</td>
                <td class="p-2 bg-slate-50 text-slate-900 text-center border-2 border-slate-900 uppercase font-extrabold">{{ categorie.label }}</td>
            </tbody>
        </table>

        <div class="flex justify-between mt-5">

            <a href="{{ path('app_categorie_index') }}" class="bg-blue-800 text-slate-50 rounded-2xl text-center px-5 py-3 hover:brightness-125 shadow-lg font-semibold">Retour à la liste</a>
            
            <div class="flex gap-3">
                <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="bg-yellow-500 text-slate-50 rounded-2xl text-center px-5 py-3 hover:brightness-125 shadow-lg font-semibold">Modifier</a>
                {{ include('categorie/_delete_form.html.twig') }}
            </div>

        </div>
    </div>
{% endblock %}
